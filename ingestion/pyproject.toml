[project]
name = "ingestion"
version = "0.1.0"
description = "Maplestory data ingestion — extract + load with dlt."
requires-python = ">=3.13"
dependencies = [
    "dlt[postgres]>=1.28.2",
]

[tool.uv]
package = false
