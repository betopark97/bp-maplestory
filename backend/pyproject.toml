[project]
name = "backend"
version = "0.1.0"
description = "Maplestory webapp FastAPI backend."
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.139.0",
    "pydantic-settings>=2.14.2",
    "uvicorn[standard]>=0.51.0",
]

[tool.uv]
package = false
