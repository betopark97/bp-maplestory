[project]
name = "transformation"
version = "0.1.0"
description = "Maplestory data transformation — dbt on postgres."
requires-python = ">=3.13"
dependencies = [
    "dbt-postgres>=1.10.2",
]

[tool.uv]
package = false
