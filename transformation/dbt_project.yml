name: "transformation"
version: "0.1.0"
config-version: 2

# Profile lives in ./profiles.yml (run dbt with --profiles-dir .).
profile: "transformation"

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

# The database carries the environment (dev_maplestory / prod_maplestory) and
# comes from the target in profiles.yml, so no model sets +database. The schema
# carries only the layer, and is the same name in every environment.
models:
  +persist_docs:
    relation: true
    columns: true
  transformation:
    staging:
      +schema: staging
      +materialized: view
    intermediate:
      +schema: intermediate
      +materialized: view
    marts:
      +schema: marts
      +materialized: view # normally materialized as table, but data is too small
